% docker

# temporal docker run
docker run --rm -it --entrypoint /bin/sh

# docker system prune, gc
docker system prune -af

# docker compose down all barusu
docker compose down --profile '*' --volumes --remove-orphans
